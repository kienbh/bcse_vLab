# /etc/nginx/conf.d/connection-upgrade.conf
# Required for proxy_set_header Connection $connection_upgrade.
# Only one map block per shared http context — install once per server.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
